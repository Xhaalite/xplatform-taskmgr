{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for local telemetry dashboard",
  "windows": ["main"],
  "permissions": ["core:default"]
}
